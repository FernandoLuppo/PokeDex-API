// import * as dotenv from "dotenv"
// import { PokemonApi, PokemonUrl, PokemonUser } from "../../../../src/service"
// import { mockLogin, mockRegister } from "../../mock"
// dotenv.config()

// describe("PokemonUser.ts", () => {
//   describe("addPokemon", () => {
//     it("Should add a pokemon in user team", async () => {
//       const req = mockReqPokemon()
//       await mockRegister(req)
//       await mockLogin(req)

//       const url = new PokemonUrl()
//       const api = new PokemonApi(url)
//       const pokemon = new PokemonUser(req, api)

//       const result = await pokemon.addPokemon()

//       console.log(result)
//     })
//   })
//   describe("removePokemon", () => {})
//   describe("userTeam", () => {
//     it("Should return the user team", () => {})
//   })
// })
